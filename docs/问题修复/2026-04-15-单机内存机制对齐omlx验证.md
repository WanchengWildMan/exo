# 2026-04-15 单机内存机制对齐 oMLX 验证

## 需求重述（开始记录）
用户要求：
1. 先保证 exo 在单机运行时尽量达到与 oMLX 同级别的内存稳定性（重点是 prefill 阶段）。
2. 先自主验证 exo 与 oMLX 的内存管理机制是否对齐。
3. 使用接近 OpenClaw 长度的 prompt（可用用户摘录文本或随机文本）进行实测。

## 验证标准
1. 机制对齐：
- exo 具备与 oMLX 一致的关键机制：
  - 在 clear_cache 前做 synchronize
  - 使用 Metal 活跃内存指标做监控
  - prefill 期间周期性 eval + clear
  - 请求结束后的缓存回收策略
2. 单机实测：
- 单机运行长 prompt prefill 时，Metal active memory 不应无界爬升。
- 至少完成一次长 prompt 请求（或给出明确拦截原因），并输出关键内存日志。

## 执行计划
1. 代码与配置现状核对（避免重复改动）。
2. 启动单机 exo，构造长 prompt 压测。
3. 抽取日志并与 oMLX 机制逐项对照。
4. 若存在差距，做最小修复并复测。
5. 回填文档结论与后续建议。
